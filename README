Dependencies

Add @prejavascript section to template

==Update from version 6==
Change all validations to $this->setValidation()

enum is deprecated, use combo instead with manual collection

isforeign is now required for relations
toastr is required

= Known issues =

-   translations
-   INDEX
    order by en relation
    search by relation, date
-   EDIT
    validation
    mostrar correcto todo
    fechas!

## The state handed to the components

The form publishes the record under its own namespace:

```js
window.csgtCrud.state
```

It used to be published as a bare `window.state`. That name is generic enough
that anything else on the page using it collided, in both directions: the
package overwrote other code's `state`, and other code overwrote the package's.

`window.state` is still set, so components that already read it keep working,
but only when nothing else has claimed the name first — the package no longer
overwrites a `state` that was already there. **Read `window.csgtCrud.state` in
new components**; the bare alias is deprecated and goes away in the next
package version.

The state only exists if the application's layout yields the
`prejavascript` section. When it does not, the views now say so instead of
rendering an empty component.

## Requirements

Package version `7.0 Beta` lives on branch `7.x`. The branch number is the package
version, not the Laravel version.

| Requirement | Supported |
| ----------- | --------- |
| PHP | 7.2 through 8.3 |
| Laravel | 7.0 or newer |

The Laravel floor comes from `Paginator::withQueryString()`. Both are inferred from the framework APIs the code actually calls and
from linting the tree on each PHP version, not from the `composer.json`
constraint, which was never kept up to date.

## Server-side permissions

`$this->permissions` is enforced on every write action, not merely read by the index
view to hide buttons. `create()`, `edit()`, `store()`, `update()` and `destroy()`
return `403` when the matching flag is false.

```php
$this->setPermissions(['create' => true, 'update' => false, 'destroy' => false]);
```

**This changed.** The routes used to be reachable by a direct request whatever
the flags said, so the buttons were hidden but the endpoints were not.
The three flags default to `false` on this branch, so an application that never
called `setPermissions()` had every write action open and will now receive a `403` on
all of them. Declare the permissions explicitly before upgrading.

## Continuous integration

`.github/workflows/ci.yml` lints the whole `src` tree on every PHP version this
branch claims to support, on each push and pull request. It replaces a
`.travis.yml` that had stopped running: Travis ended free open-source builds in
2021, and the file still listed PHP 5.3 to 5.6 plus hhvm and invoked a `phpunit`
that was never a dependency of this branch.
